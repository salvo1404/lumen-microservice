<?xml version="1.0"?>
<ruleset name="PSR2">
    <description>Autotrader Platform PSR2 coding standard.</description>
    <file>.</file>  <!--  Scan the full folder  -->

    <!--  Custom rule for our current code base. Decide with the team  -->
    <rule ref="PSR2">
        <exclude name="Squiz.WhiteSpace.ControlStructureSpacing.SpacingAfterOpen" />
    </rule>

    <!--  Exclude  -->
    <exclude-pattern>src/vendor</exclude-pattern>
    <exclude-pattern>src/tests</exclude-pattern>
    <exclude-pattern>src/resources</exclude-pattern>
    <exclude-pattern>src/database</exclude-pattern>
    <exclude-pattern>src/storage</exclude-pattern>
    <exclude-pattern>src/_ide_helper.php</exclude-pattern>
    <exclude-pattern>src/_ide_helper_models.php</exclude-pattern>
</ruleset>
